{
  "name": "carbon-registry-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ skjaldbaka17/carbon-registry-test",
    "create-local": "graph create --node http://localhost:8020/ skjaldbaka17/carbon-registry-test",
    "remove-local": "graph remove --node http://localhost:8020/ skjaldbaka17/carbon-registry-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 skjaldbaka17/carbon-registry-test",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.46.1",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
